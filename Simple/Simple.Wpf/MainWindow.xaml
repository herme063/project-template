<Window x:Class="Simple.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Simple.Wpf"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="MainWindow" 
        Height="600" 
        Width="800"
        Closing="OnClosing">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="Exit"
                          Click="ExitMenuClick"/>
            </MenuItem>
        </Menu>

        <Grid>
            <local:EntityMasterDetailView />
            
            <!--
            This is the last children so that it is displayed on top of everything.
            This is by default modal.
            -->
            <xctk:WindowContainer x:Name="DialogContainer"
                                  HorizontalAlignment="Stretch" 
                                  VerticalAlignment="Stretch" />
        </Grid>
    </DockPanel>
</Window>
